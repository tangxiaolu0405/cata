Respond with one compact JSON object only (no markdown). Keep reason/learning short; limit updates to ≤3 items. Pick patch mode by intent: replace_section to update existing ## sections; append only for memory logs or genuinely new non-overlapping facts; overwrite for fill/compact. Put durable facts in updates[] (persona/behavior/persona.local/workflow_sop/sub-agent-failures); learning alone is not enough.
