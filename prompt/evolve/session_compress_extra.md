本轮为会话阈值压缩：action=consolidate。按「三文件互斥路由」分派 short-term：**身份/偏好** → `modes/<active_mode>/persona.md`；**项目事实/运行态** → `persona.local.md`；**流程/格式/SOP** → `modes/<active_mode>/behavior.md`。各文件内已有节用 **replace_section**，新节用 **append_section**；勿跨文件重复同义 bullet。可复用细节写入 `memory/long/learnings.md` 或 `workflow_sop.md`；short-term 归档后由 server 写入 `memory/archive/consolidated-*.md`。不要 idle；不要 patch global/*。
