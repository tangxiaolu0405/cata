name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            artifact: cata-linux-amd64
            archive_ext: tar.gz
            bin_name: cata

          - goos: darwin
            goarch: amd64
            artifact: cata-darwin-amd64
            archive_ext: tar.gz
            bin_name: cata

          - goos: darwin
            goarch: arm64
            artifact: cata-darwin-arm64
            archive_ext: tar.gz
            bin_name: cata

          - goos: windows
            goarch: 386
            artifact: cata-windows-386
            archive_ext: zip
            bin_name: cata.exe

          - goos: windows
            goarch: amd64
            artifact: cata-windows-amd64
            archive_ext: zip
            bin_name: cata.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build and package
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
          ARTIFACT: ${{ matrix.artifact }}
          ARCHIVE_EXT: ${{ matrix.archive_ext }}
          BIN_NAME: ${{ matrix.bin_name }}
          GITHUB_REF: ${{ github.ref }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          mkdir -p dist
          if [[ "${BIN_NAME}" == *.exe ]]; then
            GATEWAY_BIN="cata-gateway.exe"
          else
            GATEWAY_BIN="cata-gateway"
          fi
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF_NAME}"
          else
            VERSION="dev-$(echo "${GITHUB_SHA}" | cut -c1-7)"
          fi
          LDFLAGS="-s -w -X cata/internal/cata/version.Version=${VERSION}"
          go build -ldflags="${LDFLAGS}" -o "dist/${BIN_NAME}" ./cmd/cata
          go build -ldflags="${LDFLAGS}" -o "dist/${GATEWAY_BIN}" ./cmd/cata-gateway

          ARCHIVE="${ARTIFACT}.${ARCHIVE_EXT}"
          if [ "${ARCHIVE_EXT}" = "zip" ]; then
            (cd dist && zip -r "../${ARCHIVE}" "${BIN_NAME}" "${GATEWAY_BIN}")
          else
            chmod +x "dist/${BIN_NAME}" "dist/${GATEWAY_BIN}"
            tar -czf "${ARCHIVE}" -C dist "${BIN_NAME}" "${GATEWAY_BIN}"
          fi
          ls -la "${ARCHIVE}" dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.${{ matrix.archive_ext }}

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: gofmt check
        shell: bash
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "gofmt required for:"
            echo "$unformatted"
            exit 1
          fi

      # vet/test 只覆盖核心与 gateway：cata-desktop（Fyne/OpenGL，需 CGO）与
      # cata-pet（Wails，需 npm 前端产物 + CGO）是 GUI 卫星应用，在 CI 的 Linux
      # 无头环境下无法编译，且不在发布矩阵内（build job 只构建 cata + cata-gateway）。
      - name: go vet
        run: go vet ./internal/... ./cmd/cata/... ./cmd/cata-gateway/...

      - name: go test
        run: go test ./internal/... ./cmd/cata/... ./cmd/cata-gateway/...

      - name: go test -race (core)
        run: go test -race ./internal/cata/brain/ ./internal/cata/server/ ./internal/cata/evolve/ ./internal/llm/

  release:
    name: Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate release notes from commits
        shell: bash
        env:
          CURRENT_TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          PREV_TAG=$(git describe --tags --abbrev=0 "${CURRENT_TAG}^" 2>/dev/null || true)

          write_commit_log() {
            git log "$@" --reverse --format=%H | while IFS= read -r sha; do
              subject=$(git show -s --format=%s "$sha")
              short=$(git show -s --format=%h "$sha")
              body=$(git show -s --format=%b "$sha")
              printf -- '- %s (`%s`)\n' "$subject" "$short"
              # %b 含 commit body；多行时缩进挂在同一列表项下，避免只保留 subject 丢信息
              if [ -n "${body//[[:space:]]/}" ]; then
                while IFS= read -r line || [ -n "$line" ]; do
                  printf '  %s\n' "$line"
                done <<< "$body"
              fi
              echo
            done
          }

          {
            echo "## ${CURRENT_TAG}"
            echo
            if [ -n "${PREV_TAG}" ]; then
              echo "Changes since \`${PREV_TAG}\`:"
            else
              echo "Initial release (all commits):"
            fi
            echo
            if [ -n "${PREV_TAG}" ]; then
              write_commit_log "${PREV_TAG}..${CURRENT_TAG}"
            else
              write_commit_log
            fi
            echo
            echo "## Downloads"
            echo
            echo "- Linux / macOS: \`cata\` + \`cata-gateway\` in \`.tar.gz\`"
            echo "- Windows: \`cata.exe\` + \`cata-gateway.exe\` in \`.zip\`"
            echo
            echo '```bash'
            echo "tar -xzf cata-linux-amd64.tar.gz && ./cata && ./cata-gateway --help"
            echo "tar -xzf cata-darwin-arm64.tar.gz && ./cata"
            echo "unzip cata-windows-amd64.zip && cata.exe chat"
            echo '```'
          } > release_notes.md

          echo "---- release notes preview ----"
          head -60 release_notes.md

      - name: List release assets
        run: find artifacts -type f | sort

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_notes.md
          files: |
            artifacts/**/*
          draft: false
          prerelease: false
